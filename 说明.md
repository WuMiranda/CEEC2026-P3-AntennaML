## 物理背景

人在不同握持、贴近手机状态下，天线运行产生的各类实测数据，整理成完整数据集，再借助人工智能分类算法，让手机在日常使用时，能实时自动识别当下天线所处的受干扰状态，自动切换到最合适的天线工作模式，完成天线自适应智能调节。

核心观测数据一共五类：第一类是天线切换成闭合工作模式下的两组数据，分别是信号反射数值的实数部分 gammaIn1Re、虚数部分 gammaIn1Im，搭配该模式下天线主力工作的信号频率 closeFreqMHz；第二类是用来切换天线工作档位的开关状态标识 itState；第三类是天线原始默认工作模式下，信号反射数值的实数部分 gammaIn2Re 和虚数部分 gammaIn2Im。

**从基础原理来说，天线收发信号时都会产生信号反射现象，我们把反射信号拆分成实数、虚数两个维度放到平面坐标里观察，就能直观看出天线当前是偏向聚集信号、还是分散信号；随着工作信号频率不断变化，这些坐标点位会顺着平面里顺时针、逆时针不同方向移动，两种工作模式下点位移动方向相反，就代表天线信号状态发生了反向改变。**

依靠这些基础数据和对应的变化规律，我们既能搭建出符合真实物理运行逻辑的识别特征，训练出精准判断天线受干扰状态的智能分类模型。

## 物理背景的物理基础

反射系数：
$$
\Gamma = \frac{Z_{in}-Z_0}{Z_{in}+Z_0}
$$
ZIn：天线当前输入阻抗；Z0：系统标准阻抗（一般50Ω）

反射系数副指数形式：
$$
\Gamma = |\Gamma| \cdot e^{j\theta}
=
\operatorname{Re}(\Gamma)
+
j \cdot \operatorname{Im}(\Gamma)
$$
负载阻抗与反射系数关系：
$$
\Gamma
=
\frac{Z_L - Z_0}{Z_L + Z_0}
\
$$
什么是阻抗：电路对交流信号的阻碍总能力，记作Z，单位欧姆（交流复杂，存在相位）；实部为电阻（消耗能量），虚部为电抗（储存能量、再释放能量），虚数j，相位差；

电抗是什么：电感（低通，频率低才能通过）+电容（高通）

例子：有耗网络输入阻抗模型，：
$$
Z_{in}(f)=R_0+jX(f)
$$
复数平面偏心圆通用方程：
$$
(\Gamma_{Re}-x_0)^2+(\Gamma_{Im}-y_0)^2=\rho^2
$$

$$

\Gamma_{Re}^2+\Gamma_{Im}^2-2x_0\Gamma_{Re}-2y_0\Gamma_{Im}
=
\rho^2-x_0^2-y_0^2
$$

偏心圆的物理意义：
$$
x_0 \propto \text{等效损耗电阻}；y_0 \propto \text{固定寄生电抗}；\rho \propto \text{频变电抗变化幅度}
$$

## 图片说明：

特征三：itState，开关状态，取值为0~15，离散类别变量，实则是二进制下的0000~1111，即：4个独立开关（或控制位）的组合状态，在射频系统里的意义是：匹配网络结构选择、Tuner状态组合、每个状态 = 一个不同电路拓扑。是强控制变量，每一种itstate会改变输入阻抗、反射系数、谐振频率、匹配程度。

**itState_analysis文件夹**是探究了特征3的不同取值与label的关系：

结论：没有明确关系

特征一和特征四：

**complex_plane文件夹**是探究了特征1和4与label的关系：

结论：存在规律

存在顺时针（相位随频率下降→ 容性主导）、逆时针（相位随频率上升→ 感性（虚部为正）主导）、翻转（阻抗虚部符号结构改变 → 拓扑或谐振点跨越导致状态切换）的区别，还有不同label里面，点集中于的象限不一样等等。

具体哪个容性哪个感性还没学会。（AI分析的胡说八道）

特征一和特征四和特征三：

**embed124文件夹**探究了特征一和特征四和特征三与label的关系：

是complex_plane文件夹的升级版，颜色越浅频率越低。

特征二：

**frequency文件夹**探究了特征二与label的关系：

存在规律，采用了10分箱，特征二的取值是703~849

