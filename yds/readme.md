## plan
推理阶段必须和训练阶段使用完全一致的预处理方式。并且是分别预处理。
baseline
        --model.py 将MLP单独拆出
                默认参数仍是hidden_dims=[128, 64, 32]；dropout=0.3
        --preprocess.py 拆出CSV 读取、清洗统计、特征工程、标准化保存加载
        --train.py主流程，原训练逻辑，已修正验证测试泄露
        --inference.py（暂不做）加载模型和预处理参数做单样本推理
        --results/

        --requirements.txt
        --config.py放路径和超参数
        --evaluate.py
        datas采用yds下面的

## problem
测试集参与了训练过程中的监控
测试集清洗方式不合理，有数据泄漏风险
        --从训练集统计：- 每列中位数- 每列 IQR 截尾上下界- 频率均值/标准差- StandardScaler 均值/标准差- OneHotEncoder 类别
feature_engineering 里的 freq_normalized 和后面的 StandardScaler 重复了.重复标准化
        二选一：
        （推荐）方案 A：特征工程阶段保留原始 freq，最后统一 StandardScaler。方案 B：特征工程阶段自己归一化，然后后面不要再整体 StandardScaler。
标签没有做显式映射、OneHotEncoder(sparse_output=False) 兼容性可能有问题（scikit-learn 不同版本参数不一样。）（暂时不考虑）
特征数量可能偏多，需要做消融实验：原始特征只有 6 个；工程后变成：21 个连续特征 + itState one-hot
#### 模型里用了 BatchNorm，对单样本推理要谨慎
你用了：
nn.BatchNorm1d(h_dim)
如果模型处于 eval() 模式，单样本推理是可以的。
但如果忘记 model.eval()，单样本推理可能报错或表现异常。
所以 inference.py 里必须有：
model.eval()
另外，如果比赛推理环境极简，想进一步降低复杂度，可以考虑去掉 BatchNorm，模型变成：
Linear -> ReLU -> Linear -> ReLU -> Linear
这样更容易导出 ONNX，也更稳定。
不过是否去掉要看准确率。
#### 特征数量
特征数量可能偏多，需要做消融实验
你原始特征只有 6 个：

gammaIn1Re
gammaIn1Im
closeFreqMHz
itState
gammaIn2Re
gammaIn2Im
工程后变成：

21 个连续特征 + itState one-hot
这些特征有物理意义，比如：

反射系数幅度；
相位；
VSWR；
Return Loss；
两个端口之间的差值；
比值；
乘积。
方向是对的。

但比赛评分里有：

特征数量越少、效果越好，得分越高。

所以你后面应该做特征消融：

特征组合	说明
原始 6 维
baseline
原始 + 幅度/相位
物理基础特征
原始 + 幅度/相位 + RL
加强物理意义
去掉 VSWR
因为 VSWR 在 (
去掉比值特征
比值可能带来数值不稳定
保留 Top-K 特征
根据验证集选择
我建议优先尝试这几个轻量版本：
版本 A：
g1_re, g1_im, g2_re, g2_im, freq, itState one-hot
版本 B：
版本 A + g1_mag, g2_mag, g1_phase, g2_phase
版本 C：
版本 B + g1_rl, g2_rl, delta_mag, delta_phase
版本 D：
去掉 VSWR 和 ratio，只保留更稳定的特征
VSWR 的物理意义是好的，但数值可能不稳定，因为：
VSWR = (1 + |Gamma|) / (1 - |Gamma|)
当 ∣Gamma∣ 接近 1 时会非常大

## attention
比赛要求：模型需支持复现推理。当前代码只保存了模型，还需要：StandardScaler；OneHotEncoder；特征列名；one-hot 类别顺序；清洗参数；标签映射。
比赛评分里有：特征数量越少、效果越好，得分越高。

## discover
Top-10 重要特征:
 1. freq           : 0.4057
 2. g2_re          : 0.3822
 3. g2_im          : 0.3597
 4. g2_phase       : 0.2335
 5. g2_mag         : 0.2082
 6. g2_vswr        : 0.1953
 7. delta_phase    : 0.1652
 8. mag_product    : 0.1557
 9. g1_phase       : 0.1464
10. g1_im          : 0.1459

## data
原始文件：datasets
根据6:2:2划分训练验证测试集，路径：/work2/dhy/cqw/B/yds/datas/
结果路径/work2/dhy/cqw/B/yds/results/
废弃的数据与结果：/work2/dhy/cqw/B/yds/train_data/；/work2/dhy/cqw/B/yds/output/
曾经尝试的代码：/work2/dhy/cqw/B/yds/baseline
## target
一个轻量模型文件，比如 model.onnx 或 model.pth
一个 inference.py
可能还要有：标准化参数文件、类别映射说明、requirements.txt
## origin
研电赛

**（一）赛题描述：**

赛题3

在手机射频系统中，天线的工作状态易受到使用者头部、手部接触（User Effect）的显著影响。人体靠近天线会引起天线谐振频率偏移、阻抗失配，从而导致天线辐射效率下降、传输失配损耗增加，进一步引起整机系统性能（如 OTA、TRP、TIS 等）明显恶化。

为提升复杂使用场景下的射频性能，本赛题通过离线采集多种用户接触状态下的天线相关数据，构建多场景数据集，并引入 AI 分类模型，在在线推理阶段实时判断当前天线状态并选择最优工作态（Tuner State），从而实现自适应天线调谐。

参赛选手需基于主办方提供的数据集与标签，完成以下任务：

1\. 设计具有物理合理性的输入特征；

2\. 构建用于天线状态识别的 N 分类模型；

3\. 在满足准确率与泛化能力要求的前提下，尽可能降低模型复杂度与推理计算开销；

4\. 输出可直接用于在线推理的分类模型及推理代码。

最终目标是在复杂、未知的用户接触条件下，实现快速、准确、低成本的天线最优状态选择。

**问题类型：**

1\. 监督学习

2\. N 分类问题

3\. 模型设计要求：

(1)模型类型不限，可自由选择，但考虑到硬件部署需满足以下条件：

① 模型需为静态前向推理模型（不建议动态结构或条件分支网络）；

② 禁止使用需要序列状态维护的模型（如在线 RNN状态）；

③ 模型需支持单样本前向推理。

(2)允许使用但不限于以下模型结构：

① MLP（多层感知机）

② 轻量级CNN/1D-CNN

③ 树模型（如决策树、GBDT的推理等价形式）

④ 其他可导出为静态推理图的模型结构

(3)不建议使用的模型与行为：

① 依赖外部数据库或查表的大规模搜索模型；

② 推理阶段重新训练或更新参数；

③ 使用测试集信息进行模型设计或调参。

**任务描述：**

1\. 输入：当前天线状态相关的一维特征向量（float32）

2\. 输出：最优天线态编号（0 ~ N-1）

模型需在未知使用者姿态、手握方式、头部靠近等场景下具备良好的泛化能力。

**（二）输出要（提交格式与目录结构见附录一输出要求）：**

1\. 系统方案介绍 PPT；

2\. 技术说明文档，需至少包含：

(1) 输入特征选择思路及物理意义说明；

(2) 模型结构设计与选择理由；

(3) 模型参数量、MACs、FLOPs 的估算方法与结果；

(4) 推理时间测试环境、测试方法与统计方式；

(5) 模型泛化能力提升的关键设计说明；

(6) 量化或模型压缩方案说明（如有）

3\. 训练完成的分类模型文件（格式不限，如 .pth / .onnx / .pt / .h5 等），需支持复现推理；

4\. 推理代码：提供模型加载及单样本推理接口。

**（三）标准：满分100分，额外附加分30分，补充条款见附录二评审标准：**

1\. 模型准确率与泛化性(50分)：主要考察测试集分类准确率（Top-1 Accuracy）准确率、不同场景子集上的稳定性表现；整体准确率基线 > 90%

2\. 输入特征选择能力 (10分)：主要考察特征是否具有物理合理性及特征维度是否冗余，特征数量越少、效果越好，得分越高；

3\. 模型大小（20分）：主要考察模型参数量、模型文件大小，在准确率接近的情况下，模型越小得分越高；模型文件大小基线 < 50KB

4\. 推理时间与计算量（20分）：主要考察MACs与FLOPs（本赛题中模型结构统一为 MLP，在单样本 CPU 推理场景下，模型推理时间与 MACs 数量高度相关）

5\. 加分项：量化（INT8 / 定点化）模型设计（20分）、对错误分类样本的分析与解释（5分），提升泛化性能关键设计与说明（5分）

**（四）专家答疑邮箱：**

zhaoyifan4@huawei.com

**（五）茶思屋互动交流答疑社区：**

https://www.chaspark.com/#/races/competitions/1237842156721336320

**（六）附录一输出要求：**

1\. 提交格式与目录结构（强制）  
 submission/  
  ├── model/  
  │   └── model.xxx 训练完成的分类模型  
  ├── infer/  
  │   └── inference.py 推理代码  
  ├── doc/  
  │   ├── technical_report.pdf 技术说明文档  
  │   └── solution_intro.pptx 系统方案介绍PPT  
  ├── README.md 其他说明（可选）  
  └── requirements.txt 依赖

**（七）附录二评审标准：**

1\. 【评测方式说明】

准确率与泛化能力由主办方在统一环境、隐藏测试集上自动评测；

参数量、模型大小、MACs / FLOPs 由官方脚本统计复检；

推理时间由选手报告提供，主办方对前列模型进行抽检复测；  
 最终评分以主办方评测结果为准。

2\. 【违规处理】

(1)使用违规模型结构；

(2)提交内容与技术说明严重不符；

(3)抽检结果与报告存在显著偏差；

(4)发生测试集泄漏等行为。

上述情况将直接取消参赛成绩。