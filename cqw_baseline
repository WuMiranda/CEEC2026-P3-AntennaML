特征一实部：gammaIn1Re，闭环态的反射系数实部	
特征一虚部：gammaIn1Im，闭环态的反射系数虚部	
特征二：closeFreqMHz，闭环态的中心频点	
  系统响应发生在哪里
特征三：itState，开关状态	
  0~15，0000~1111
特征四实部：gammaIn2Re，默认态的反射系数实部	
特征四虚部：gammaIn2Im，默认态的反射系数虚部	
## 公式
Γ = |Γ|·e^(jθ) = Re(Γ) + j·Im(Γ)
|Γ|（模值）：表示反射波与入射波的幅度比，范围 0~1。|Γ|=0 表示完全匹配，|Γ|=1 表示全反射。
θ（相角）：表示反射波相对于入射波的相位偏移。
Γ = (Z_L - Z₀) / (Z_L + Z₀) 
Z₀（传输线特征阻抗）通常是纯实数
Z_L（负载阻抗，例如天线的输入阻抗）则一般是复数：Z_L = R + jX（R 为电阻，X 为电抗）。
闭环态：
  根据r知道阻抗失配的具体性质：到底是电阻偏大/偏小，还是电抗呈容性/感性。
  驱动可调电容/电感，同时调整匹配网络的电阻和电抗分量，使 Γ 趋近于 0（即 Smith 圆图的中心点）。
  闭环态的反射系数是一个随时间变化的复数，系统不断优化它的实部和虚部。
默认态：
  系统预先存储一组固定的 Γ 值（也是复数），对应某个预设的天线匹配状态。
  通常这个默认态 Γ 的模值 |Γ| 较低（例如 -10 dB 回波损耗，对应 |Γ|≈0.316），但不一定是最优的。它的实部与虚部在设计时已固定。
## 物理
  系统以默认态启动，保证通信。随即切换到闭环态，不断监测并优化反射系数。当闭环态因故无法达到最佳时，系统回退到默认态，作为安全基线。
  反射系数（Γ）是射频工程中衡量信号在传输线上传输效率的关键指标。它量化了有多少信号能量因阻抗不匹配而从负载端（如天线）被反射回源端（如发射机）


